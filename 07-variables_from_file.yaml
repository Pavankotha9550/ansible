- name: variables from file
  hosts: local
  connection: local
  vars_files:
    varfile.yaml
  tasks:
  - name: printing from other file
    ansible.builtin.debug:
      msg: "hello {{ name }} and age was {{ age }}"