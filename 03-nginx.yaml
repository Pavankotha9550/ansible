- name: nginx install
  hosts: frontend
  become: yes
  tasks:
  - name: installing nginx server
    ansible.builtin.package:
      name: nginx
      state: present
  - name: start nginx server
    ansible.builtin.service:
      name: nginx
      state: start
      enable: yes